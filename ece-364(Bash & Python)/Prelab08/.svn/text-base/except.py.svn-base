import os
import sys
import math
num_list = input('Please enter some values: ')
#num_list = num_list.replace(' ',',')
num_list = num_list.split(' ')
new_list=[]
for x in num_list:
    try:
        new_list.append(float(x))
    except ValueError:
        continue
#print(new_list)
print('The sum is: %0.1f' % sum(new_list))

